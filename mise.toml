[tools]
python = "3.12"
bun = "1.3"

[tasks.setup]
description = "Register all extensions in this repo with Pi's global settings.json"
run = "python3 scripts/install_extensions.py"

[tasks.test]
description = "Run all extension tests"
run = "bun install && bun test"
